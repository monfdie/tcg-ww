<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Manage: <%= tour.title %></title>
    <link rel="stylesheet" href="/style.css">
    <style>
        body { padding: 40px; background: #0b0d17; max-width: 800px; margin: 0 auto; }
        h1 { color: #d4af37; border-bottom: 1px solid #333; padding-bottom: 10px; }
        .section { background: #161c30; padding: 25px; border-radius: 8px; margin-bottom: 30px; border: 1px solid #2a3454; }
        label { display: block; color: #8da4c4; margin-bottom: 5px; margin-top: 15px; }
        input, textarea { width: 100%; padding: 10px; background: #0a0c14; border: 1px solid #333; color: #fff; border-radius: 4px; box-sizing: border-box; }
        button { margin-top: 15px; padding: 12px 25px; cursor: pointer; border: none; font-weight: bold; border-radius: 4px; }
        .btn-save { background: #4facfe; color: #000; }
        .btn-post { background: #d4af37; color: #000; }
    </style>
</head>
<body>

    <a href="/admin/dashboard" style="color: #666; text-decoration: none;">← Back to Dashboard</a>
    <h1>Manage: <%= tour.title %></h1>

    <div class="section">
        <h2 style="color:#fff; margin-top:0;">🔗 Links & Rules</h2>
        <form action="/admin/update-links" method="POST">
            <input type="hidden" name="slug" value="<%= tour.slug %>">
            
            <label>Registration Link:</label>
            <input name="regLink" value="<%= tour.regLink %>" placeholder="https://...">

            <label>Rules Link (RU):</label>
            <input name="rulesLinkRu" value="<%= tour.rulesLinkRu %>" placeholder="https://...">

            <label>Rules Link (EN):</label>
            <input name="rulesLinkEn" value="<%= tour.rulesLinkEn %>" placeholder="https://...">

            <button type="submit" class="btn-save">Update Links</button>
        </form>
    </div>

    <div class="section">
        <h2 style="color:#fff; margin-top:0;">📢 Post Announcement (Telegram Style)</h2>
        <form action="/admin/announce" method="POST" enctype="multipart/form-data">
            <input type="hidden" name="slug" value="<%= tour.slug %>">
            
            <label>Message Text:</label>
            <textarea name="text" rows="6" placeholder="Write your post here..." required></textarea>

            <label>Image (Optional):</label>
            <input type="file" name="image" accept="image/*">

            <button type="submit" class="btn-post">Publish Post</button>
        </form>
    </div>

    <div class="section">
        <h2 style="color:#fff; margin-top:0;">🎮 Link Match to Tournament</h2>
        <form action="/admin/link-match" method="POST">
            <input type="hidden" name="slug" value="<%= tour.slug %>">
            <label>Room Code (e.g. A1B2):</label>
            <input name="roomId" required placeholder="XXXX">
            <button type="submit" style="background:#333; color:#fff;">Link Match</button>
        </form>
    </div>

</body>
</html>
