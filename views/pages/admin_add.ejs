<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin: Add Tournament</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        body { overflow-y: auto; padding: 50px; }
        .form-container { max-width: 600px; margin: 0 auto; background: #161c30; padding: 30px; border-radius: 8px; }
        label { display: block; margin-top: 15px; color: #8da4c4; }
        input, select { margin-top: 5px; }
    </style>
</head>
<body>
    <div class="form-container">
        <h1 style="color: #fff;">Add New Tournament</h1>
        <form action="/admin/add" method="POST">
            
            <label>Slug (URL name, unique):</label>
            <input name="slug" required placeholder="e.g. gitcg-cup-3">

            <label>Title:</label>
            <input name="title" required placeholder="Tournament Name">

            <label>Date Text:</label>
            <input name="date" required placeholder="Feb 25, 2026">

            <label>Prize:</label>
            <input name="prize" required placeholder="100 $">

            <div style="display: flex; gap: 10px;">
                <div style="flex:1">
                    <label>Region:</label>
                    <select name="region">
                        <option value="AS">AS</option>
                        <option value="EU">EU</option>
                        <option value="NA">NA</option>
                    </select>
                </div>
                <div style="flex:1">
                    <label>System:</label>
                    <select name="system">
                        <option value="Draft">Draft</option>
                        <option value="Allpick">Allpick</option>
                    </select>
                </div>
            </div>

            <label>Registration Link:</label>
            <input name="regLink" required placeholder="https://...">

            <label>Card Style:</label>
            <select name="cardStyle">
                <option value="blue">Blue (Standard)</option>
                <option value="gold">Gold (Featured/Cup)</option>
            </select>

            <label>Badge Text:</label>
            <input name="badgeText" value="OPEN">

            <label>Auto-Hide Date (Optional):</label>
            <input type="date" name="visibleUntil">

            <button type="submit" class="btn-primary" style="margin-top: 20px; width: 100%;">Post Tournament</button>
        </form>
    </div>
</body>
</html>
