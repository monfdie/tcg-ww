<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin Panel: Create Post</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        body { overflow-y: auto; padding: 40px; background: #0b0d17; }
        .form-container { 
            max-width: 600px; margin: 0 auto; background: #161c30; 
            padding: 40px; border-radius: 12px; border: 1px solid #2a3454; 
            box-shadow: 0 10px 40px rgba(0,0,0,0.5);
        }
        
        h1 { color: #fff; text-align: center; margin-top: 0; margin-bottom: 30px; font-family: 'Cinzel', serif; }

        label { display: block; margin-top: 18px; color: #a0b4d0; font-size: 0.9em; margin-bottom: 5px; }
        input, select, textarea { 
            width: 100%; padding: 12px; background: #0f131f; border: 1px solid #3c4566; 
            color: white; border-radius: 6px; box-sizing: border-box; font-family: 'Segoe UI', sans-serif;
        }
        input:focus, select:focus, textarea:focus { border-color: #d4af37; outline: none; }

        .upload-zone {
            border: 2px dashed #3c4566; padding: 20px; text-align: center; border-radius: 6px;
            transition: 0.3s; background: rgba(0,0,0,0.2);
        }
        .upload-zone:hover { border-color: #d4af37; background: rgba(212, 175, 55, 0.05); }

        .submit-btn {
            margin-top: 30px; width: 100%; padding: 15px; font-size: 1.2em; font-weight: bold;
            border: none; border-radius: 6px; cursor: pointer; text-transform: uppercase; transition: 0.2s;
            background: linear-gradient(45deg, #d4af37, #f2d06b); color: #000;
        }
        .submit-btn:hover { box-shadow: 0 0 20px rgba(212, 175, 55, 0.5); transform: translateY(-2px); }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>📢 New Announcement</h1>
        
        <form action="/admin/add" method="POST" enctype="multipart/form-data">
            <input type="hidden" name="type" value="announcement">

            <label>Title (Headline):</label>
            <input name="title" required placeholder="Ex: Update 4.0 or Tournament Results">

            <label>Image (Required for News):</label>
            <div class="upload-zone">
                <input type="file" name="image" accept="image/*" required style="border: none; background: transparent;">
            </div>

            <label>Post Text / Content (Optional):</label>
            <textarea name="description" rows="8" placeholder="Write your news here..."></textarea>
            
            <label>Slug (Optional ID):</label>
            <input name="slug" placeholder="Leave empty to auto-generate">

            <label>External Link (Optional - e.g. YouTube):</label>
            <input name="regLink" placeholder="https://...">

            <button type="submit" class="submit-btn">PUBLISH POST</button>
        </form>
        
        <div style="text-align: center; margin-top: 20px;">
            <a href="/admin/dashboard" style="color: #666; text-decoration: none;">Cancel & Return</a>
        </div>
    </div>
</body>
</html>
