<%- include('../partials/header') %>

<style>
    .tierlist-wrapper { max-width: 1000px; margin: 0 auto; padding-bottom: 50px; }
    .tl-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; border-bottom: 2px solid #3c4b70; padding-bottom: 15px; }
    .tl-title { font-family: 'Cinzel', serif; color: #fff; font-size: 2.2em; margin: 0; }
    
    .btn-action { background: linear-gradient(135deg, #4facfe, #00f2fe); color: #050b14; font-weight: bold; border: none; padding: 10px 22px; border-radius: 4px; cursor: pointer; font-size: 14px; text-transform: uppercase; letter-spacing: 1px; transition: 0.2s; box-shadow: 0 4px 10px rgba(0,0,0,0.3); }
    .btn-action:hover { transform: translateY(-2px); box-shadow: 0 6px 15px rgba(79, 172, 254, 0.4); }

    /* Острые углы и более светлый фон самого тирлиста */
    #capture-tierlist { background: #151b2b; border: 2px solid #3c4b70; border-radius: 0; overflow: hidden; }
    .tier-row { display: flex; border-bottom: 1px solid #2a3454; min-height: 80px; background: rgba(30, 41, 59, 0.7); }
    .tier-row:last-child { border-bottom: none; }
    
    /* Шрифты тиров классические, темный текст */
    .tier-label { width: 100px; min-width: 100px; display: flex; align-items: center; justify-content: center; font-family: 'Segoe UI', sans-serif; font-size: 24px; font-weight: 800; color: #333; user-select: none; border-right: 1px solid #151b2b; }
    
    /* Классические цвета TierMaker */
    .tier-t0 { background: #ff7f7f; } /* S - Красный/Лососевый */
    .tier-t1 { background: #ffbf7f; } /* A - Оранжевый */
    .tier-t2 { background: #ffdf7f; } /* B - Желто-оранжевый */
    .tier-t3 { background: #ffff7f; } /* C - Желтый */
    .tier-t4 { background: #bfff7f; } /* D - Светло-зеленый */
    
    .tier-dropzone { flex: 1; display: flex; flex-wrap: wrap; gap: 8px; padding: 10px; align-content: center; min-height: 80px; }

    /* Более светлые карточки игроков */
    .player-card { position: relative; width: 125px; height: 38px; background: linear-gradient(135deg, #2c3a58, #1e293b); border: 1px solid #4facfe; border-radius: 4px; display: flex; align-items: center; justify-content: center; padding: 0 8px; user-select: none; transition: 0.15s ease; box-shadow: 0 4px 10px rgba(0,0,0,0.4); }
    .player-card.draggable { cursor: grab; }
    .player-card.draggable:active { cursor: grabbing; border-color: #00f2fe; }
    .player-card:hover { border-color: #00f2fe; background: #35466b; }

    .player-name { color: #ffffff; font-size: 13px; font-weight: 600; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; text-align: center; width: 100%; }
    
    /* Индикаторы стрелочек */
    .trend-icon { position: absolute; right: -6px; top: -6px; font-size: 11px; font-weight: bold; background: #151b2b; border: 1px solid #3c4b70; border-radius: 50%; width: 18px; height: 18px; display: flex; align-items: center; justify-content: center; }
    .trend-up { color: #2ecc71; border-color: #2ecc71; }
    .trend-down { color: #e74c3c; border-color: #e74c3c; }
    .trend-none { display: none; }

    .pool-section { margin-top: 30px; background: rgba(30, 41, 59, 0.7); border: 1px dashed #4facfe; border-radius: 4px; padding: 15px; display: none; }
    .pool-title { color: #4facfe; font-size: 12px; font-weight: bold; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 15px; }
    .pool-dropzone { display: flex; flex-wrap: wrap; gap: 8px; min-height: 90px; }

    .create-own-box { text-align: center; margin-top: 40px; padding-top: 30px; border-top: 1px solid #3c4b70; }

    .add-player-box { margin-top: 20px; display: flex; gap: 10px; display: none; }
    .add-player-box input { flex: 1; padding: 12px; background: #151b2b; color: #fff; border: 1px solid #3c4b70; border-radius: 4px; outline: none; }
    .add-player-box input:focus { border-color: #4facfe; }
</style>

<div class="tierlist-wrapper">
    <div class="tl-header">
        <h1 class="tl-title" id="page-title">OFFICIAL TIERLIST</h1>
        <button class="btn-action" onclick="downloadTierlist()">SAVE IMAGE</button>
    </div>

    <div id="capture-tierlist">
        <% const tiers = ['t0', 't1', 't2', 't3', 't4']; %>
        <% tiers.forEach((tier, index) => { %>
        <div class="tier-row">
            <div class="tier-label tier-<%= tier %>">T<%= index %></div>
            <div class="tier-dropzone" id="<%= tier %>">
                <% tierlistData[tier].forEach(player => { %>
                    <div class="player-card">
                        <span class="player-name"><%= player.name %></span>
                        <div class="trend-icon trend-<%= player.trend %>"><%= player.trend === 'up' ? '▲' : (player.trend === 'down' ? '▼' : '') %></div>
                    </div>
                <% }) %>
            </div>
        </div>
        <% }) %>
    </div>

    <div class="pool-section" id="pool-container">
        <div class="pool-title">Unassigned Players</div>
        <div class="pool-dropzone" id="unassigned">
            <% tierlistData.unassigned.forEach(player => { %>
                <div class="player-card">
                    <span class="player-name"><%= player.name %></span>
                </div>
            <% }) %>
        </div>

        <div class="add-player-box" id="user-add-player-box">
            <input type="text" id="new-player-input" placeholder="Type new player name here to add them to your tierlist..." onkeypress="if(event.key === 'Enter') addPlayer()">
            <button class="btn-action" onclick="addPlayer()">+ ADD</button>
        </div>
    </div>

    <div class="create-own-box" id="create-own-container">
        <p style="color: #8da4c4; margin-bottom: 15px; font-size: 16px;">Want to share your own vision of the meta?</p>
        <button class="btn-action" style="background: transparent; border: 1px solid #4facfe; color: #4facfe; box-shadow: none;" onclick="enableBuilderMode()">CREATE YOUR OWN</button>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>

<script>
    function enableBuilderMode() {
        document.getElementById('page-title').innerText = "MY TIERLIST";
        document.getElementById('pool-container').style.display = 'block';
        document.getElementById('user-add-player-box').style.display = 'flex';
        document.getElementById('create-own-container').style.display = 'none';

        const unassignedPool = document.getElementById('unassigned');
        
        document.querySelectorAll('.player-card').forEach(card => {
            const trend = card.querySelector('.trend-icon');
            if(trend) trend.remove();
            
            card.classList.add('draggable');
            if(card.parentElement.id !== 'unassigned') {
                unassignedPool.appendChild(card);
            }
        });

        const zones = ['t0', 't1', 't2', 't3', 't4', 'unassigned'].map(id => document.getElementById(id));
        zones.forEach(zone => {
            new Sortable(zone, {
                group: 'tierlist',
                animation: 150,
                ghostClass: 'sortable-ghost',
                touchStartThreshold: 5
            });
        });
    }

    function addPlayer() {
        const input = document.getElementById('new-player-input');
        const name = input.value.trim();
        if (!name) return;

        const card = document.createElement('div');
        card.className = 'player-card draggable';
        card.innerHTML = `<span class="player-name">${name}</span>`;
        
        document.getElementById('unassigned').appendChild(card);
        input.value = '';
    }

    async function downloadTierlist() {
        const area = document.getElementById('capture-tierlist');
        const canvas = await html2canvas(area, { backgroundColor: '#151b2b', scale: 2 });
        const link = document.createElement('a');
        link.download = 'gitcg_tierlist.png';
        link.href = canvas.toDataURL('image/png');
        link.click();
    }
</script>

<%- include('../partials/footer') %>
