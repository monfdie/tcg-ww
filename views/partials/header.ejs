</head>
<body>

<nav class="navbar">
    <div class="nav-brand">GITCG <span>DRAFT</span></div>
    <div class="nav-links">
        <a href="/" class="<%= path === '/' ? 'active' : '' %>">Main</a>
        <a href="/create" class="<%= path === '/create' ? 'active' : '' %>">Create Draft</a>
        <a href="/tournaments" class="<%= path === '/tournaments' ? 'active' : '' %>">Tournaments</a>
        <a href="/history" class="<%= path === '/history' ? 'active' : '' %>">History</a>
    </div>
    
    <div class="nav-user">
        <% if (user) { %>
            <div style="display: flex; align-items: center; gap: 10px;">
                <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar" width="32" style="border-radius: 50%; border: 1px solid #4facfe;">
                <span style="color: #fff; font-family: 'Segoe UI', sans-serif; font-weight: bold;"><%= user.global_name || user.username %></span>
                <a href="/logout" style="color: #ff6b6b; font-size: 0.85em; text-decoration: none; margin-left: 10px;">Logout</a>
            </div>
        <% } else { %>
            <a href="/auth/discord" class="login-btn" style="text-decoration: none;">Discord Login</a>
        <% } %>
    </div>
</nav>

<div class="page-container">
