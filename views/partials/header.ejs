<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Segoe+UI&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
    <style>
        .navbar {
            display: flex; justify-content: space-between; align-items: center;
            padding: 15px 50px; background: rgba(10, 12, 20, 0.95);
            border-bottom: 2px solid #3c4566; box-shadow: 0 5px 15px rgba(0,0,0,0.5);
            font-family: 'Cinzel', serif;
        }
        .nav-brand { font-size: 1.5em; font-weight: bold; color: #fff; letter-spacing: 2px; }
        .nav-brand span { color: #4facfe; }
        .nav-links { display: flex; gap: 30px; }
        .nav-links a {
            color: #8da4c4; text-decoration: none; font-size: 1.1em;
            text-transform: uppercase; transition: 0.3s;
        }
        .nav-links a:hover, .nav-links a.active { color: #fff; text-shadow: 0 0 10px #4facfe; }
        .nav-user .login-btn {
            background: transparent; border: 1px solid #7289da; color: #7289da;
            padding: 8px 15px; border-radius: 4px; font-family: 'Segoe UI', sans-serif;
            cursor: pointer; transition: 0.3s;
        }
        .nav-user .login-btn:hover { background: rgba(114, 137, 218, 0.2); }
        .page-container { padding: 40px; }
    </style>
</head>
<body>

<nav class="navbar">
    <div class="nav-brand">GITCG <span>DRAFT</span></div>
    <div class="nav-links">
        <a href="/" class="<%= path === '/' ? 'active' : '' %>">Main</a>
        <a href="/create" class="<%= path === '/create' ? 'active' : '' %>">Create Draft</a>
        <a href="/tournaments" class="<%= path === '/tournaments' ? 'active' : '' %>">Tournaments</a>
        <a href="/history" class="<%= path === '/history' ? 'active' : '' %>">History</a>
    </div>
    <div class="nav-user">
        <button class="login-btn">Discord Login</button>
    </div>
</nav>

<div class="page-container">
